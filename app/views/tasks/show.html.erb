<h2><%= @task.title %></h2>

<p>Task details: <%= @task.details %></p>
<p>Is the task complete? <%= @task.complete ? "✅" : "❌"  %></p>

<p><%= link_to "Back to task list", tasks_path %></p>

<p><%= link_to "Edit this task", edit_path(@task)%></p>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
